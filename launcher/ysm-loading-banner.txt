# Каждая строка ниже - причина. Меняется файл - лаунчер один раз выключает
# в инстансе каждого игрока баннер загрузки моделей Yes Steve Model
# (DisableLoadingStateScreen); дальше настройка снова принадлежит игроку.
#
# Баннер видит только хозяин мира: строку "Model Sync State: Loading" мод
# зажигает на ветке isLocalServer, а снять её может лишь ответ его нативной
# части, который в этой сборке не приходит. Модели при этом не появляются
# никогда, и надпись висит весь сеанс.
2026-08-23: баннер загрузки моделей YSM больше не висит у хозяина мира
