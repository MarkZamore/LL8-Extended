modLoader = "javafml"
loaderVersion = "[4,)"
license = "All Rights Reserved"
issueTrackerURL = "https://github.com/MarkZamore/LANMinecraft/issues"

[[mixins]]
config = "ll8guifit.mixins.json"

[[mods]]
modId = "ll8guifit"
version = "1.0.0"
displayName = "LL8 GUI Fit"
authors = "LL8 Extended"
description = '''
Gives the inventory player preview room for this build's three-dimensional
armour. The preview is clipped to its panel, and a helmet taller than the
vanilla head box was cut off at the top; the player is now drawn at three
quarters of the vanilla scale inside the same panel, so the whole outer
armour layer fits.
'''

[[dependencies.ll8guifit]]
modId = "neoforge"
type = "required"
versionRange = "[21.1,)"
ordering = "NONE"
side = "CLIENT"

[[dependencies.ll8guifit]]
modId = "minecraft"
type = "required"
versionRange = "[1.21.1,1.22)"
ordering = "NONE"
side = "CLIENT"
